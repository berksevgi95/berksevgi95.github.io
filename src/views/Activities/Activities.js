import React from 'react';

const Activities = ({
    id
}) => {
    return (
        <article id={id} className="h-full overflow-auto">
            asljd
        </article>
    )
}

export default Activities;