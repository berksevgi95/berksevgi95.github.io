import React from 'react';

const Projects = ({
    id
}) => {
    return (
        <article id={id} className="h-full overflow-auto">
            asljd
        </article>
    )
}

export default Projects;