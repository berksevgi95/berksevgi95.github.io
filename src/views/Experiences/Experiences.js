import React from 'react';

const Experiences = ({
    id
}) => {
    return (
        <article id={id} className="h-full overflow-auto">
            asljd
        </article>
    )
}

export default Experiences;